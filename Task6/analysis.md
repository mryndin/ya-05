# Отчёт по результатам анализа Kubernetes Audit Log

## Подозрительные события

1. Доступ к секретам:
   - Кто: docker-for-desktop
     - Где: Namespace `kube-system`, Секрет: `все`
     - Почему подозрительно: Попытка несанкционированного чтения токенов или паролей.
   - Кто: docker-for-desktop
     - Где: Namespace `kube-system`, Секрет: `все`
     - Почему подозрительно: Попытка несанкционированного чтения токенов или паролей.
   - Кто: docker-for-desktop
     - Где: Namespace `kube-system`, Секрет: `все`
     - Почему подозрительно: Попытка несанкционированного чтения токенов или паролей.
   - Кто: docker-for-desktop
     - Где: Namespace `kube-system`, Секрет: `все`
     - Почему подозрительно: Попытка несанкционированного чтения токенов или паролей.

2. Привилегированные поды:
   - Кто: docker-for-desktop
     - Комментарий: Создан под `privileged-pod` в пространстве `secure-ops` с флагом `privileged: true`, дающим доступ к хост-системе.

3. Использование kubectl exec в чужом поде:
   - Кто: docker-for-desktop
     - Что делал: Выполнил подключение (exec) к поду `coredns-66bc5c9577-8nxt5` в пространстве `kube-system`.
   - Кто: docker-for-desktop
     - Что делал: Выполнил подключение (exec) к поду `coredns-66bc5c9577-8nxt5` в пространстве `kube-system`.
   - Кто: docker-for-desktop
     - Что делал: Выполнил подключение (exec) к поду `coredns-66bc5c9577-8nxt5` в пространстве `kube-system`.

4. Создание RoleBinding с правами cluster-admin:
   - Кто: docker-for-desktop
     - К чему привело: Предоставление полных административных прав (`cluster-admin`) сервисному аккаунту.
   - Кто: docker-for-desktop
     - К чему привело: Предоставление полных административных прав (`cluster-admin`) сервисному аккаунту.

5. Удаление audit-policy.yaml:
   - Событий не обнаружено

## Вывод

В ходе анализа файла логов аудита были зафиксированы явные маркеры компрометации кластера. 
Злоумышленник (или скрипт имитации) предпринял попытку горизонтального перемещения и повышения привилегий, создав под с неограниченными правами на хост и привязав роль `cluster-admin` к рядовой учетной записи. 
Для защиты кластера рекомендуется внедрить строгие политики RBAC и запретить запуск подов с флагом `privileged`.
