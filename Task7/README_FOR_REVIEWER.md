# Отчет по Заданию 7: Аудит и Безопасность

### Выполненные шаги:
1. Создан namespace `audit-zone` с принудительной политикой `PodSecurity restricted`.
2. Подготовлены нарушающие манифесты в `insecure-manifests/`.
3. Подготовлены исправленные манифесты в `secure-manifests/` (проходят проверку PSA и OPA).
4. Настроен OPA Gatekeeper для блокировки privileged, hostPath и root-пользователей.
5. Настроена политика аудита API-сервера.

### Как проверить:
- Запустите `./verify/verify-admission.sh`, чтобы увидеть блокировку опасных подов.
- Примените исправленные поды из `secure-manifests/`.